# Mouse support
set -g mouse on

# Scroll history
set -g history-limit 10000

# Refresh the status bar frequently enough for lightweight system stats.
set -g status-interval 2
set -g status-right '#(~/.local/bin/tmux-system-monitor)'
