# User Preferences

## Code Lookup
Use jcodemunch-mcp for code lookup whenever available. Prefer symbol search, outlines, and targeted retrieval over reading full files. Only fall back to `Read` on full files when jcodemunch can't provide what's needed.
